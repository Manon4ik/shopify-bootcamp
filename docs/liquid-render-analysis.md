Разбери: {% render 'card-product' %} на примере theme\mountain-coffee\sections\related-products.liquid

Кто вызывает?
Что передает?
Что получает обратно?
Почему это похоже на функцию?


Вызывает цикл recommendation при построении сетки рекомендованных продуктов. Передает recommendation продукта и настройки секции. Обратно получает html карточки продукта (карточек в цикле).
Похоже на функцию тк есть входящие значения и возвращаемый реультат.

==========

Professional Challenge

Выбери один snippet из Dawn.

Например: price.liquid Объясни его работу человеку, который знает PHP, но никогда не видел Shopify.


1. в {% comment %} идет описание снипета и входящих параметров
2. в зависимости от типа продукта - присваиваются значения переменной target
3. далее создаются все переменные для этого снипета
4. происходит проверка при наличии target и placeholder и далее отрисовка html
5. добавляется доп класс в зависимости от значений и условий
6. отрисовка блока с обычной ценой в зависимости от условий
7. отрисовка блока с скидочной ценой в зависимости от условий
8. выбор первого значения вариативного продукта (если таковой)
9. отображения значков для скидки / проданно при выполнении условий


==========

Senior Challenge

Ответь письменно почему Shopify рекомендует render вместо include.

- лучше поддерживаемость кода
- лучше оптимизация
- огрницечнная область видимость переменных тем самым предотвращение возвращения измененных значений базовых переменных.
- отход от дублируемости кода и правок в нескольких местах